import { useVaultStore } from '@/store/useVaultStore';

// Re-export specific parts or the whole store if needed.
// For now, we just alias it so existing imports work seamlessly.
export const useVault = useVaultStore;
